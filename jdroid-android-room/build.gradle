apply from: "$rootDir/module.gradle"

ext.PROJECT_NAME = "Jdroid Android - Room"
description = "Jdroid dependency project for Room integration"

buildscript {
	apply from: "$rootDir/build_dependencies.gradle"
	dependencies {
		classpath(libs.jdroid_gradle_android_plugin)
	}
}

dependencies {

	api project(":jdroid-android-core")

	api(libs.androidx_room_runtime)

	testImplementation(libs.junit)
	testImplementation(libs.robolectric)
}
